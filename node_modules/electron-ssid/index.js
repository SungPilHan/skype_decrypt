var addon = require('./build/Release/electron-ssid.node');

module.exports = {
    getSSID: function () {
        return new Promise(function(resolve, reject) {
            addon.getSSID(ssid => {
                resolve(ssid);
            });
        });
    }
}
