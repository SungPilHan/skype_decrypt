// @ts-check

const { join, resolve } = require('path');
const { fetchBinariesFromPackage, getBranchName, getPlatformBranchName } = require('@skype/ivy-downloader');

fetchBinariesFromPackage({
    name: 'desktop-idle',
    branch: getBranchName({ name: 'desktop-idle', electronBranch: 'skype-3.0' }),
    revision: '1.1.1.2',
    binaries: [join('build', 'Release', 'desktopIdle.node')]
}, resolve(__dirname, '..'));
