exports.getCurrentLocation = function getCurrentLocation() {
    try {
        let location = require(`./lib/${process.arch}/location.node`);
        return location.getCurrentLocation();
    }
    catch(e) {
        return Promise.reject(e);
    }
};
